/**
 * Dev Nav Entry Point
 * ===================
 * CSS-only entry for the root developer navigation page.
 * No app logic here — the real app starts at src/pages/home/.
 */

import '../sass/index.scss';
